apiVersion: v1
kind: Service
metadata:
  name: {{ include "randrust.fullname" . }}
  labels:
{{ include "randrust.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: UDP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "randrust.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
